[phases.setup]
nixPkgs = ['nodejs_18', 'npm']

[phases.install]
cmds = [
    'rm -rf node_modules package-lock.json',
    'npm install --legacy-peer-deps'
]

[phases.build]
cmds = ['npm run build']

[start]
cmd = 'npm run preview'
